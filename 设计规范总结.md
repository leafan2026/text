# 售后 SOP · 设计规范与经验总结

> 这是「售后标准工作流程」HTML 文档的最终版设计规范沉淀。
> 包含设计哲学、令牌系统、组件规范、关键交互、踩坑经验和决策思路。

---

## 一、设计哲学

参考 [Apple 中国官网](https://www.apple.com.cn/mac/) 的设计语言：

1. **极简克制**：能不用的视觉元素都不用。无装饰渐变、无投影、无第二种强调色。
2. **摄影优先（在文档场景退化为「内容优先」）**：UI 后退，让内容自身呼吸。
3. **明暗 tile 交替制造节奏**：白 ↔ parchment（极浅米色）两种相似色作为章节背景交替，颜色变化本身就是分隔器。
4. **单一 Action Blue**：`#0066cc` 只用于真正可点击的元素，是「click me」信号，不作装饰。
5. **typography 是骨架**：用字号 / 字重 / 字距而不是色彩对比来建立层级。

---

## 二、设计令牌（Design Tokens）

### 颜色

```css
--primary:        #0066cc;  /* Action Blue 唯一交互色 */
--primary-focus:  #0071e3;

--ink:            #1d1d1f;  /* 主文字，比纯黑略浅 */
--ink-80:         #333333;  /* 次级文字 */
--ink-48:         #6e6e73;  /* 弱化文字、表头、提示文字 */
--ink-30:         #86868b;  /* 提示线、辅助灰 */

--canvas:         #ffffff;  /* 主背景（odd 章节）*/
--parchment:      #f5f5f7;  /* 次背景（even 章节）+ 卡片色 */
--surface-pearl:  #fafafc;
--surface-black:  #000000;  /* 仅 global nav */

--divider:        #f0f0f0;  /* 表格行分隔 */
--hairline:       #e0e0e0;  /* 边框 */
--line:           #c7c7cc;  /* 流程图连线 */
```

**核心原则**：
- 文字只用四级灰（ink → ink-80 → ink-48 → ink-30）
- 蓝色只一种，且只用在可点击元素上
- 卡片背景永远和章节背景**反色**（详见 §六）

### 字体

```css
--font-display: "SF Pro Display", system-ui, -apple-system,
                BlinkMacSystemFont, "Helvetica Neue",
                "PingFang SC", "Microsoft YaHei", sans-serif;

--font-text:    "SF Pro Text",    system-ui, -apple-system,
                BlinkMacSystemFont, "Helvetica Neue",
                "PingFang SC", "Microsoft YaHei", sans-serif;
```

**关键判断**：
- SF Pro Display 用于 ≥21px 的标题，SF Pro Text 用于 <21px 的正文
- 字重梯度：**400 / 600**，no 500（Apple 故意跳过 500）
- 正文 17px（不是 16px），line-height 1.47，letter-spacing -0.374px——这是 Apple 「reading 而不是 scanning」的节奏
- 标题字距收紧到 -0.012em ～ -0.018em，制造「Apple tight」头条感
- **特例**：右滑提示用「仿宋」(`FangSong, STFangsong, Songti SC, serif`)，让 UI hint 退到更克制的衬线体

### 间距

```css
--s-xs:       8px;
--s-sm:      12px;
--s-md:      17px;   /* 与正文 line-height 倍数对齐 */
--s-lg:      24px;
--s-xl:      32px;
--s-xxl:     48px;
--s-section: 80px;   /* tile 内边距标准 */
```

### 圆角

```css
--r-sm:    8px;     /* 小型工具按钮 */
--r-md:   11px;     /* pearl button、小卡片 */
--r-lg:   18px;     /* 表格、流程图容器 */
--r-pill: 9999px;   /* 主按钮、流程图节点 */
```

---

## 三、布局结构

### 三层结构

```
<body>
  <nav class="global-nav">                     ← 44px 黑色顶栏
  <div class="nav-overlay">                    ← 移动端抽屉遮罩
  <div class="doc">                            ← Grid: sidebar + main
    <aside class="sidebar">                    ← 280px parchment sidebar
    <main class="content">
      <div class="content-inner">
        <header class="doc-header">            ← 标题区
        <section>...</section>                 ← 多个章节（交替背景）
```

### 章节边到边铺色

关键决策：**不能让 `.content` 有横向 padding**，否则 section 不能延伸到 viewport 边缘。

```css
.content { padding: 0; min-width: 0; }
.content-inner { max-width: none; min-width: 0; }

main section {
  padding: 72px var(--s-section);   /* 章节自己控制内边距 */
  margin: 0;
  background: var(--canvas);        /* 默认白 */
}

/* 内容居中：直接子级限定宽度 */
main section > * {
  max-width: var(--content-max);    /* 820px */
  margin-left: auto;
  margin-right: auto;
}

/* 偶数章节用 parchment */
main section:nth-of-type(even) {
  background: var(--parchment);
}
```

> ⚠️ **`min-width: 0` 必加**——grid item 默认 `min-width: auto`，里面的 flex 流程图会把 grid 列宽撑到 max-content，造成 viewport 横向溢出（正文 `<p>` 文字被切）。

---

## 四、Sidebar 文档导航

### 层级规则

- **不用 section title 文字**（如「三个分流」「关键规则」）——文字标题让分组显得悬浮，反而层级混乱
- 用 **22px 空白 + 0.5px hairline 分隔线** 表达分组
- 父项 14px / `ink-80`，子项 13px / `ink-48`，颜色梯度区分层级
- 子项前加 6×1px 灰色短横线作为视觉指引，代替单纯的 padding-left

```css
.toc-group {
  margin-top: 22px;
  padding-top: 22px;
  border-top: 1px solid var(--divider);
}
.toc a.sub {
  padding-left: 26px;
  font-size: 13px;
  color: var(--ink-48);
  position: relative;
}
.toc a.sub::before {
  content: '';
  position: absolute;
  left: 14px;
  top: 50%;
  width: 6px;
  height: 1px;
  background: var(--ink-30);
  opacity: 0.5;
}
```

---

## 五、卡片 surface 反色规则

> **核心规则**：卡片背景永远和章节背景**反着来**。

| 元素 | 白章节 (odd) | parchment 章节 (even) |
|---|---|---|
| `.flow-canvas` 流程图 | parchment | **white** |
| `.tbl` 表格 | 透明（继承白）| **white** |
| `.notice` 注释条 | parchment | **white** |
| `.sub-block` 子区块 | parchment | **white** |

实现：

```css
main section:nth-of-type(even) .flow-canvas,
main section:nth-of-type(even) .tbl,
main section:nth-of-type(even) .notice,
main section:nth-of-type(even) .sub-block {
  background: var(--canvas);
}
```

这条规则让每个卡片永远有明确的边界，不会与章节融合。

---

## 六、自建横向流程图组件

### 为什么不用 Mermaid

- Mermaid 的默认样式（节点小、字体不一致、连线粗、字号 default）和我们的整页设计风格冲突
- 即使深度调教 themeCSS / themeVariables，仍受限于 svg 渲染、节点尺寸自动计算、文字字体
- **彻底用 HTML + CSS 自建**：节点是和页面同一套 `--font-text` 字体的 pill 元素、连线是 1.25px 灰色 + CSS 三角箭头、可完全控制

### 节点类型

```html
<span class="step step--terminal">业务通过</span>   <!-- 黑底白字 pill，起止 -->
<span class="step">改状态</span>                    <!-- 白底 hairline 边，普通 -->
<span class="step step--decision">退款/换货</span>  <!-- 白底 1.5px 黑边，决策 -->
<span class="step step--output">生成退款单</span>   <!-- parchment 底 600 weight，产出物 -->
<span class="step step--special">跳转·回仓判定</span> <!-- 黑底白字，跳转锚点 -->
<a    class="step step--link" href="#after">调货后</a> <!-- 蓝色边，可点击链接 -->
<span class="step step--muted">参考依据</span>      <!-- dashed 边，参考信息 -->
<span class="step step--stack">                    <!-- 双行节点 -->
  <span>修改原订单状态</span>
  <span class="step-sub">例：尺码 130 → 140</span>
</span>
```

**关键设计**：Action Blue 只用在 `.step--link`（真正的可点击锚点）。所有「强调」节点改用 `.step--output`（parchment + 600 weight），靠字重和浅底突出，不破坏 Apple 单一蓝色规则。

### 连接线

```html
<span class="line"></span>            <!-- 28px 水平箭头 -->
<span class="line line--long"></span> <!-- 40px -->
```

```css
.line {
  width: 28px;
  height: 1.25px;
  background: var(--line);
  position: relative;
}
.line::after {
  /* CSS 三角箭头 */
  content: '';
  position: absolute;
  right: -1px; top: 50%;
  transform: translateY(-50%);
  border-left: 5px solid var(--line);
  border-top: 4px solid transparent;
  border-bottom: 4px solid transparent;
}
```

### 分支 / 合流

```html
<div class="branches">
  <div class="branch">
    <span class="branch-label">退款</span>
    <span class="line"></span>
    <span class="step">改状态</span>
  </div>
  <div class="branch">
    <span class="branch-label">换货</span>
    <span class="line"></span>
    <span class="step">改状态</span>
  </div>
</div>
<span class="merge"></span>           <!-- 合流标记 -->
<span class="step step--terminal">工单结束</span>
```

`.branches` 用 CSS `::before` `::after` 自动画出 `[` 形分叉竖线，`.merge` 同理画 `]` 形合流。完全靠 CSS，不依赖 SVG。

---

## 七、横向滚动 + 右滑提示

### 滚动容器的位置

**关键决策**：滚动容器是 `.flow`，不是 `.flow-canvas`。

```css
.flow-canvas {
  position: relative;
  overflow: hidden;          /* 锚点容器 */
}
.flow {
  display: flex;
  overflow-x: auto;          /* 真正的滚动容器 */
  width: 100%;
}
```

这样 `.scroll-hint-pill` 可以作为 `.flow-canvas` 的子元素绝对定位在右边缘，**不会跟着内容横滑**。

### 右滑提示：渐变 + 仿宋竖排

```css
.scroll-hint-pill {
  position: absolute;
  top: 0; right: 0; bottom: 0;
  width: 40px;
  background: linear-gradient(
    to right,
    rgba(245, 245, 247, 0) 0%,
    rgba(174, 174, 178, 0.10) 55%,
    rgba(174, 174, 178, 0.22) 100%
  );
  opacity: 0;
  transition: opacity 0.3s ease;
  pointer-events: none;
}
.scroll-hint-pill .hint-text {
  writing-mode: vertical-rl;          /* 文字竖排 */
  text-orientation: upright;          /* 汉字正立 */
  font-family: "FangSong", "STFangsong", "Songti SC", serif;
  font-size: 13px;
  font-weight: 400;
  color: var(--ink-48);
  letter-spacing: 0.18em;
}
```

**经验**：
- 渐变要轻——`alpha 0.22` 终点已经足够暗示「右边还有内容」
- 文字色用 `ink-48`（深灰）而不是 `ink`（纯黑），免得太重
- 仿宋字体让 UI hint 退到衬线感，与无衬线主体形成微妙对比，更克制
- 渐变起点色要跟随容器底色（odd 章节 parchment 起点，even 章节 white 起点），避免边缘色差

### 显示逻辑

```js
function update(canvas) {
  var scroller = canvas.querySelector('.flow');
  var hasOverflow = scroller.scrollWidth > scroller.clientWidth + 2;
  var atEnd = scroller.scrollLeft + scroller.clientWidth >= scroller.scrollWidth - 4;
  canvas.classList.toggle('has-overflow', hasOverflow);
  canvas.classList.toggle('at-end', atEnd);
}
```

CSS `.flow-canvas.has-overflow:not(.at-end) .scroll-hint-pill { opacity: 1 }` 控制显隐。

监听四个时机做兜底：`scroll` / `resize` / `load` / `fonts.ready` + 一次 `requestAnimationFrame` + 300ms `setTimeout`。字体加载完会改变节点宽度，重测一次必须。

---

## 八、滚动 reveal 动效

### 关键决策：避免首屏闪烁

直接在 `<head>` 内同步给 `<html>` 加 `.has-reveal` class，**在 CSS 加载前就生效**：

```html
<head>
  <title>...</title>
  <script>
    document.documentElement.classList.add('has-reveal');
  </script>
  <style>...</style>
</head>
```

```css
.has-reveal .doc-header,
.has-reveal main section {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 0.85s cubic-bezier(0.32, 0.72, 0, 1),
              transform 0.85s cubic-bezier(0.32, 0.72, 0, 1);
  will-change: opacity, transform;
}
.has-reveal main section.is-in {
  opacity: 1;
  transform: translateY(0);
}
```

> `cubic-bezier(0.32, 0.72, 0, 1)` 是 iOS 系统缓动曲线，比 default ease 更符合 Apple 节奏。

### IntersectionObserver 触发

```js
var obs = new IntersectionObserver(function (entries) {
  entries.forEach(function (entry) {
    if (entry.isIntersecting) {
      entry.target.classList.add('is-in');
      obs.unobserve(entry.target);    // 一次性触发
    }
  });
}, {
  threshold: 0.08,
  rootMargin: '0px 0px -6% 0px'       // 提前触发，更自然
});
```

### 无障碍

```css
@media (prefers-reduced-motion: reduce) {
  .has-reveal main section { opacity: 1; transform: none; transition: none; }
}
```

---

## 九、移动端适配

### 汉堡抽屉 sidebar

- ≤833px：sidebar 改为 `position: fixed` 抽屉，默认 `translateX(-100%)` 藏在屏外
- 顶部黑栏左侧加 36×36 汉堡按钮，点击 toggle `.open` class
- 配半透明遮罩 + `backdrop-filter: blur(2px)`
- 点击遮罩、按 Esc、点 sidebar 内链接、resize 变宽都会自动关闭
- 打开时 `body.overflow = hidden` 锁定背景滚动
- 三横线 → × 用 CSS transform 旋转动画

### 流程图：保持横向不翻转

> 曾尝试在移动端把流程图翻转为纵向卡片——**否决**。
>
> 原因：纵向后每个分支变成卡片，但「分支后还要合并到下一步」的逻辑无法用纵向清晰表达，节奏感丢失。
>
> 最终方案：移动端流程图保持横向 + 横向滚动 + 右滑提示，节点字号 / padding / 连线长度同步缩小。

### 字号梯度

| 元素 | 桌面 | ≤833px | ≤480px |
|---|---|---|---|
| h1 | 40px | 30px | 26px |
| h2 | 28px | 24px | 21px |
| h3 | 21px | 18px | 17px |
| p | 17px | 16px | 15.5px |
| 流程节点 | 14px | 13.5px | 13px |
| 连线长度 | 28px | 22px | 18px |
| 表格单元格 | 12×16/15px | 10×12/14px | 9×10/13px |
| 提示条宽度 | 40px | — | 32px |

---

## 十、关键经验（踩坑总结）

### 1. Grid item 的 `min-width: auto` 陷阱

**症状**：移动端正文 `<p>` 从右边被切掉，viewport 横向滚动。

**原因**：CSS Grid 的 grid item 默认 `min-width: auto`——按内容 max-content 计算最小宽度。`<main>` 里有横向 flex 流程图，被算成最小宽度 1000+px，于是 `<main>` 撑爆，正文也跟着溢出。

**修复**：

```css
.content       { min-width: 0; }
.content-inner { min-width: 0; }
body { overflow-x: hidden; }   /* 兜底 */
```

### 2. 流程图横向滚动容器要放对位置

**错误**：把 `overflow-x: auto` 放在 `.flow-canvas` 上。
**结果**：滑动条出现在 canvas 边缘，提示 pill 跟着内容滚动，定位失效。

**正确**：让 `.flow-canvas` `overflow: hidden + position: relative`，把 `overflow-x: auto` 下放到内层 `.flow`。这样 hint pill 作为 canvas 的子元素绝对定位时不会被滚动影响。

### 3. 渐变起点色要跟随容器底色

```css
/* 容器是 parchment 时，渐变起点也用 parchment alpha 0 */
background: linear-gradient(to right, rgba(245, 245, 247, 0), ...);

/* 容器是 white 时，渐变起点改用 white alpha 0 */
background: linear-gradient(to right, rgba(255, 255, 255, 0), ...);
```

虽然 alpha 0 视觉上都是透明，但渐变中间点的 RGB 插值会带入起点色，影响过渡平滑度。

### 4. 不要用 `transparent` 关键字做渐变起点

`transparent` 在 CSS 里等于 `rgba(0, 0, 0, 0)`，渐变中间点会引入黑色调子。永远显式写 `rgba(<容器底色>, 0)`。

### 5. 首屏 reveal 闪烁的解法

如果在 `<body>` 底部 JS 才给 `<html>` 加 `.has-reveal`，浏览器会先 paint 一次完整内容，再隐藏，造成闪烁。

**正确**：在 `<head>` 内 inline `<script>` 同步加 class——这一行代码会在 CSS 解析前执行，确保隐藏样式从首屏就生效。

### 6. Apple 单一蓝色规则不容妥协

每次想用 Action Blue 强调一个产出节点（生成退款单、生成虚拟移仓单等）时，都要克制——蓝色只属于「click」。改用：
- `step--output`：parchment + 600 weight，靠字重突出
- `step--special`：黑底，靠反色突出
- `step--terminal`：黑底，起止节点用

把蓝色保留给真正可点击的元素（`step--link` 的三个阶段锚点、文字链接、sidebar 链接、表头超链接）。

---

## 十一、设计取舍记录

### 「新功能」专区被否决

第一版做了一个 hero 区 + 4 个新能力专屏（六仓轮询、二次复核、回仓判定、拆快递审核）。

否决原因：售后流程文档不是营销页，新功能不该作为主线。最终把这些信息合并到详细操作的对应章节里，附录处用「与 v4 的差异」表格标 ★。

### 流程图横向 vs 纵向

移动端尝试过纵向卡片堆叠，被否决——分支后的合流无法清晰表达。最终保留横向 + 横向滚动 + 右滑提示。

### Mermaid 被全面替换

尝试过 Mermaid + 深度 themeCSS 调教，但节点尺寸、字体加载、连线粗细仍不一致。彻底改用 HTML/CSS 组件后，整页设计才真正统一。

### 「右滑查看」用仿宋

最初提示 pill 用 SF Pro Text 600 + 纯黑底白字，太抢眼。换成深灰渐变 + ink-80 文字仍偏重。最终改为**仿宋衬线 + ink-48 灰** + 浅渐变——UI hint 退到最克制的角色，符合 Apple「无干扰」哲学。

### 章节交替背景的节奏

奇数白 / 偶数 parchment 的硬性规则带来一个争议：关键规则的 4 个子章节也会交替。最终保留这个规则，理由：节奏感比逻辑分组优先级更高，4 个 sub-section 颜色交替反而带出阅读节奏。

---

## 十二、文件结构

```
售后标准工作流程.html    ← 单文件，所有 CSS/JS inline
售后标准工作流程.md      ← Markdown 源文档（同内容）
设计规范总结.md          ← 本文档
```

部署到 GitHub Pages：
- 仓库：[leafan2026/text](https://github.com/leafan2026/text)
- 入口：`https://leafan2026.github.io/text/`（自动渲染 index.html）
- 中文路径：`https://leafan2026.github.io/text/售后标准工作流程.html`

---

## 十三、可复用的核心组件

如果未来要写新的内部文档，可以直接复用：

1. **整套 design tokens**（colors / fonts / spacing / radius）
2. **sidebar + main content 的 grid 布局**
3. **章节交替背景 + 卡片反色规则**
4. **流程图组件**（`.step` / `.line` / `.branches` / `.merge` / `.paths` / `.path-tag`）
5. **横向滚动 + 右滑提示**（仿宋竖排 + IntersectionObserver）
6. **滚动 reveal 动效**（`.has-reveal` class + IntersectionObserver）
7. **移动端汉堡抽屉 sidebar**

把这些 CSS / JS 抽成一个 base layer，新文档只需要替换内容部分即可。

---

*文档版本：v1 · 2026-05*
